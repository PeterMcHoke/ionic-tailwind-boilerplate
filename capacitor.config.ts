import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'blank-ionic-tailwind',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
